"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

var _path = require("path");

var _assert = _interopRequireDefault(require("assert"));

var _chalk = _interopRequireDefault(require("chalk"));

var _workboxWebpackPlugin = _interopRequireDefault(require("workbox-webpack-plugin"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function _default(api, options) {
  const pkg = api.pkg,
        relativeToTmp = api.relativeToTmp;
  (0, _assert.default)(pkg && pkg.name, `You must have ${_chalk.default.underline.cyan('package.json')} and configure ${_chalk.default.underline.cyan('name')} in it when enable pwa.`);

  if (process.env.NODE_ENV === 'production') {
    api.addEntryCode(`
require('${relativeToTmp((0, _path.join)(__dirname, './registerServiceWorker.js'))}');
    `.trim());
    const mode = options.workboxPluginMode || 'GenerateSW';
    const defaultGenerateSWOptions = mode === 'GenerateSW' ? {
      cacheId: pkg.name
    } : {};

    const config = _objectSpread({
      exclude: [/\.map$/, /favicon\.ico$/, /manifest\.json$/]
    }, defaultGenerateSWOptions, options.workboxOptions || {});

    api.chainWebpackConfig(webpackConfig => {
      webpackConfig.plugin('workbox').use(_workboxWebpackPlugin.default[mode], [config]);
      webpackConfig.resolve.alias.set('register-service-worker', require.resolve('register-service-worker'));
    });
  } // TODO: 更新 html 文件

}